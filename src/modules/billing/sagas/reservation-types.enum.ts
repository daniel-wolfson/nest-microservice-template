export enum ReservationType {
    FLIGHT = 'FLIGHT',
    HOTEL = 'HOTEL',
    CAR = 'CAR',
}

export enum EventType {
    FLIGHT_REQUESTED = 'reservation.flight.requested',
    HOTEL_REQUESTED = 'reservation.hotel.requested',
    CAR_REQUESTED = 'reservation.car.requested',
}
